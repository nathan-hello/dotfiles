# OpenCode Global Agent Instructions

## TypeScript
- Always use `bun run tsc` for TypeScript type checking, never use `npx tsc --noEmit`
- Use `bun run typecheck` if your project has that script defined
